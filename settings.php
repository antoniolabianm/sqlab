<?php

// Prevent PHP scripts from being accessed directly from the browser.
defined('MOODLE_INTERNAL') || die();

if ($ADMIN->fulltree) {
    // Currently no settings to configure, prepared for future use.
}
